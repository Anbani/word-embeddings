"""Vendored, stdlib-only modules copied verbatim from sibling Anbani repos.

Kept here (rather than imported across submodules) so stage-1 can run in an
isolated Docker container that only mounts this repo. Do not edit locally —
if the upstream changes, re-copy. Provenance is noted at the top of each file.
"""
